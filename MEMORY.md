# MEMORY.md

Component-local memory for tatara-cli. Cross-repo decisions live in each
repo's own `MEMORY.md`; there is no parent repo.

Format: `YYYY-MM-DD - decision/finding`

---

## Decisions

2026-05-27 - repo bootstrapped; no go.sum yet (no deps); Dockerfile copies only go.mod (not go.sum) to avoid build error on empty module.
2026-05-27 - helmlint pre-commit hook omitted (no chart in this repo); yamllint charts/ exclude omitted for same reason.
2026-05-27 - revive `exported` rule disabled in .golangci.yml; CLAUDE.md hard rule prohibits docstrings on new code.

2026-05-27 - RefreshToken takes *http.Client param (nullable) so tests can inject httptest server without changing public contract; spec's 4-arg form would have required real issuer in tests.
2026-05-27 - DeviceFlow.TickOverride (zero=use device-code interval) lets tests run at 1ms without changing Poll's production behaviour or API surface.
2026-05-27 - gosec G304/G115/G117 nolint'd on intentional file-path-from-variable and uintptr->int casts; rationale in-line.

2026-05-27 - v0.1.0 code-complete on main: 5 subcommands wired, 13 MCP tools, full TDD coverage, CI green. Release (tag push) deferred until tap repo + secrets are in place.
2026-05-27 - Pre-argo Wave 6.1 release preconditions (Homebrew tap repo, HOMEBREW_TAP_GITHUB_TOKEN, separate HARBOR creds) are obsolete. Superseded by argo migration entry below: tap dropped, tatara-cli release runs in cluster as `tatara-go-release` CWT with creds from tatara ns secrets.
2026-05-27 - CI/release migrated from .github/workflows to argo workflows. Push to main triggers tatara-go-ci CWT in tatara ns. Tag push (tag_pushed event) triggers tatara-go-release CWT. goreleaser config trimmed to amd64-only (linux + darwin); Homebrew tap dropped, user manages binary install manually from github release tarballs. Wave-6 preconditions in earlier MEMORY entries are SUPERSEDED: tap repo and HOMEBREW_TAP_GITHUB_TOKEN no longer required. Harbor robot creds and github status PAT live in tatara ns via tatara-argo-workflows helm release.

2026-06-13 - change_summary tool POSTed snake_case body keys (pr_title, pr_body, delivered_scope, remaining_scope) but the operator changeSummaryReq json tags are camelCase and decodeJSON sets DisallowUnknownFields, so those fields were silently rejected (lifecycle fell back to default MR title/body). Fixed: tool args stay snake_case (agent-facing), Build maps them to camelCase body keys. Single-word tools (issue_outcome/pr_outcome) were unaffected since their keys collide. Added most_problematic (change_summary) + plan (issue_outcome) for issue#6 lifecycle notifications.

2026-06-28 - semver push-CD (spec 2026-06-28): change_summary gains REQUIRED enum change_significance (major|minor|patch, snake arg -> camel wire key changeSignificance) - the operator derives the PR's semver:* label + auto-merge gate from it. Vocab is major (not "breaking"). Added .github/workflows/release.yml: push-to-main, gated on lint/test/build, then cd-release@tatara-helmfile tag (cuts cli vX.Y.Z off the merged PR's semver:* label), build.sh re-publishes image at :vX.Y.Z (git describe resolves the fresh tag), then cd-release bump rewrites the WRAPPER's 3 TATARA_CLI_VERSION pins (Dockerfile ARG + Makefile + .github/ci/build.sh) lockstep in one PR. Push trigger (not workflow_run) so the action sees the merge commit as $GITHUB_SHA. Seed tag floor = existing v0.4.0 (git tag --sort=-v:refname picks it; no new tag cut here). Needs secrets.TATARA_CD_TOKEN (bot PAT).

## Dead-ends / things tried that did not work
2026-06-13 - gosec G703-G705 taint-analysis rules excluded in .golangci.yml - flag pre-existing internal/auth/clientcreds.go (already nolint-justified).


2026-05-27 - golangci-lint-action@v6 cannot install golangci-lint v2 (must use @v7); v2.1.6 was built with go1.24 and refuses go1.25 modules; v2.12.x tightened config schema and rejects our legacy `disable-all` / `exclude-rules` / string `local-prefixes`. Stable config: golangci-lint v2.11.4 via action@v7 with `verify: false`. Fixing the config to be strict-v2-clean is a v0.1.1 boy-scout task.

## Open questions

*(nothing yet)*

2026-06-07 - Dockerfile had two build blockers fixed before first push: (1) `COPY go.mod ./` missing `go.sum` - deps failed to download; fixed to `COPY go.mod go.sum ./`. (2) binary placed at `/tatara` not `/usr/local/bin/tatara` - wrapper `COPY --from=tatara-cli` couldn't find it; fixed to install at `/usr/local/bin/tatara`. Committed on main (SHA 9cb6fa6) then built/pushed as tatara-cli:0.4.0. GO_VERSION=1.25 from Dockerfile matched go.mod `go 1.25.5` and built successfully.

2026-06-09 - **Headless OIDC auth (A1).** `internal/auth/clientcreds.go` adds `ClientCredentialsToken` (discovery + grant) and `AccessToken(ctx)` which prefers stored login token, falls back to client_credentials when OIDC_ISSUER/CLI_OIDC_CLIENT_ID/CLI_OIDC_CLIENT_SECRET are set (in-memory cache, 30s pre-expiry refresh), returns ErrNoToken otherwise. `tatara raw` and `tatara mcp` now call `AccessToken` on ErrNoToken from LoadToken so both work in agent pods with no `tatara login`. Token path/refresh/save wiring is preserved for stored tokens; skipped for client-creds tokens (re-mint via AccessToken on next call). SHA 3de66ae.

2026-06-06 - **Operator MCP tools.** Added 9-tool group (project/repo/task/subtask) targeting tatara-operator REST API. `Tool` now carries `Target` (TargetMemory|TargetOperator); `Server` holds two clients (memory + operator) and dispatches by target. Operator base URL resolves flag > env(TATARA_OPERATOR_URL) > file(operatorBaseUrl yaml key) > default (https://tatara.szymonrichert.pl/api/v1/operator). Single OIDC token assumed to carry both tatara-memory and tatara-operator audiences (Keycloak audience mapper added in operator M6); revisit if a separate audience-scoped token flow is needed. Ships as tatara-cli v0.4.0.

2026-06-09 - **SCM intent MCP tools (cli T2).** Added 3 operator-targeted tools (9->12): `propose_issue` -> `POST /projects/{TATARA_PROJECT}/issues` body `{repositoryRef,title,body,kind}` (kind bug|improvement); `review_verdict` -> `POST /tasks/{task|TATARA_TASK}/review` body `{decision,body,suggestions}` (decision approve|request_changes|comment); `pr_outcome` -> `POST /tasks/{task|TATARA_TASK}/pr-outcome` body `{action,reason}` (action merge|close). All are CRD-writes via the operator REST API; the operator TaskReconciler is the SOLE SCM egress path (agents emit intent only). project/task resolved via argOrEnv(TATARA_PROJECT/TATARA_TASK) exactly like existing operator tools; propose_issue accepts repo|repositoryRef arg, body key is always repositoryRef. Wire shapes match contract lock 2026-06-09 sections 3+4 byte-for-byte. Ships as tatara-cli v0.5.0.

2026-06-12 - **.golangci.yml strict-v2 enforcement (v0.1.1, task-chp8d).** The v1 leftovers from the 2026-05-27 dead-end (`disable-all` / `exclude-rules` / string `local-prefixes`) were already migrated to `default: none` / `linters.exclusions.rules` / gci `sections: [..., prefix(...)]` by the bootstrap commit, so the config is strict-v2-clean. The remaining gap was enforcement: added `golangci-lint config verify` as the first step of `make lint` and a `repo: local` pre-commit hook gated on `^\.golangci\.yml$`, so schema drift fails fast going forward. `gosec.excludes` G304/G115/G117 left in place (redundant with the inline `//nolint:gosec` at every suppressed site, but removing them risks surfacing un-nolinted findings in `run`, unverifiable without the toolchain). Still open externally in `tatara-argo-workflows`: flip the CI golangci-lint step's `verify: false` workaround to `true` once it pins a version that recognises G117.

2026-06-12 - **E2E MCP protocol smoke harness (task-8rg6c).** `internal/mcp/e2e_test.go` (`TestE2E_StdioProtocol`) drives the registered server over the real JSON-RPC protocol via mcp-go's `client.NewInProcessClient(srv.srv)` (same dispatch path as `ServeStdio`, no subprocess): Start -> Initialize -> ListTools (asserts len == AllTools+OperatorTools and a sample of names) -> CallTool against faked tatara-memory (POST /memories) and tatara-operator (GET /tasks/{task}) backends, plus a backend-500 -> IsError result. This catches the 0.4.x class of break (tools/list marshalling, dispatch/target routing, result-marshalling) that registration-only tests cannot. Result text extracted via `mcplib.AsTextContent`. Boy-scout: made `TestOperatorTools_RequireArgs` / `TestOperatorTools_SCMRequireArgs` hermetic with `t.Setenv("TATARA_TASK"/"TATARA_PROJECT","")` - they assert the no-env error path but the agent runtime injects those vars, so they failed under ambient env (CI runs clean, so this was latent). NOTE: `internal/cmd` raw tests (`TestRaw_NoTokenSurfacesErrNoToken`, `TestRaw_GETPrintsBodyAndStatus`) have the same ambient-env coupling (CLI_OIDC_CLIENT_ID/OIDC_ISSUER/TATARA_*_URL injected -> client-creds path engages instead of ErrNoToken); left unfixed (out of scope, separate package), green in a clean env.

2026-06-12 - **Chat MCP tools (tatara-cli#24).** Added a 10-tool group targeting tatara-chat REST: `chat_create_room`/`chat_list_rooms`/`chat_get_room`/`chat_close_room` (rooms), `chat_add_participant`/`chat_list_participants`/`chat_remove_participant` (participants), `chat_send_message`/`chat_poll_messages`/`chat_get_log` (messages). New `TargetChat` (third Target); `Server` now holds three clients (memory+operator+chat) and `NewServer(memory, operator, chat, log)` dispatches via a switch in `clientFor`. Chat base URL resolves flag(--chat-base-url) > env(TATARA_CHAT_URL) > file(chatBaseUrl yaml key) > default (https://tatara.szymonrichert.pl/api/v1/chat); wired exactly like operator (single OIDC token assumed to carry the tatara-chat audience too - same assumption as operator M6, revisit if a separate audience-scoped token is needed). Wire shapes mirror tatara-chat `internal/chat/handler.go` byte-for-byte: room id is the `{id}` path param (tool arg `room_id`), `chat_send_message` body `{participant_id,body,target?,kind?}`, `chat_poll_messages` passes `?participant=<id>` (poll advances the participant cursor; response carries `room_status`/`has_more` so a loop knows when to stop), `chat_get_log` optional `after`/`limit` -> `next` cursor. e2e test extended with a faked chat backend + `chat_create_room` round-trip. Tools prefixed `chat_` (memory/operator are unprefixed) for namespace clarity - `create_room`/`send_message` are too generic bare.

2026-06-09 - **CRITICAL: tools/list marshal bug broke ALL MCP tools (0.4.x).** `internal/mcp/server.go register()` built every tool with `mcplib.NewTool(name, WithDescription, WithRawInputSchema)`. mcp-go `NewTool` seeds a default object `InputSchema` (Type="object"); `WithRawInputSchema` also sets `RawInputSchema`. mcp-go `Tool.MarshalJSON` errors when BOTH are set, so `tools/list` returned an error and agents loaded ZERO tatara tools - `claude mcp list` still showed "Connected" (initialize handshake only), masking it. This was the real cause of agent runs Succeeding with 0 subtasks / empty comments / "partially working" PR feedback, NOT auth (auth probes were curl-only, bypassing claude). Fix: `buildTool()` uses `mcplib.NewToolWithRawSchema(name, desc, schema)` (RawInputSchema only). Regression test `TestBuildTool_AllToolsMarshal` json.Marshals every tool. Verified at protocol level: tools/list now returns all 32 tools, zero stderr. Ships as tatara-cli 0.4.3. Lesson: "MCP server Connected" != "tools exposed"; always verify tools/list, not just the handshake.

2026-06-13 - **internal/cmd tests made hermetic (task scan-srx2k, PR #36).** Closed the gap flagged in the 2026-06-12 e2e entry: `internal/cmd` command tests coupled to ambient runtime env. The agent/CI runner injects TATARA_PROJECT, TATARA_MEMORY_URL and OIDC_ISSUER/CLI_OIDC_CLIENT_ID/CLI_OIDC_CLIENT_SECRET, so `TestRaw_GETPrintsBodyAndStatus` saw a `/tatara` project prefix on the request path, and `TestRaw_NoTokenSurfacesErrNoToken` / `TestMCP_ErrsWhenNoToken` engaged the client-credentials path (real token minted) instead of returning ErrNoToken - all three failed CI. Fix: added `internal/cmd/main_test.go` with a package `TestMain` that `os.Unsetenv`s the injected vars (TATARA_TASK/PROJECT/MEMORY_URL/OPERATOR_URL/CHAT_URL, OIDC_ISSUER, CLI_OIDC_CLIENT_ID/SECRET, XDG_CONFIG_HOME/STATE_HOME) once before the suite, mirroring the per-test `t.Setenv("...","")` pattern the mcp package already uses. Tests now green under both ambient and clean env.

2026-06-13 - **query/describe mode enum trimmed to backend contract (tatara-cli#21).** The `query` tool advertised `enum:["local","global","hybrid","naive","mix","bypass"]` but tatara-memory's `/queries` `QueryMode.Valid()` only accepts local/global/hybrid/naive, so `mode:"mix"` (and bypass) returned `400 invalid mode` - the advertised schema and backend disagreed. Trimmed both `query` and `describe` enums to the four valid modes (describe previously had no enum at all, same backend `Valid()` gate). Regression test `TestQueryDescribe_ModeEnumMatchesBackend`. Symptom 2 of the issue (empty `matches` for valid modes) was a tatara-memory bug (Service.Query omitted include_references), fixed there, not in the CLI passthrough.

2026-06-14 - mise toolchain added: .mise.toml pins go=1.25.5 + golangci-lint=2.12.2 (min_version=2026.6.3); CI uses jdx/mise-action@v2; no build-essential needed (CGO_ENABLED=0).

2026-06-13 - **`tatara status` diagnostic subcommand (task-q4v9f).** Added read-only `internal/cmd/status.go` (sixth subcommand) that answers "am I authenticated, and where am I pointed?" with zero network I/O. Auth state derived locally: stored token at `auth.DefaultTokenPath()` -> "logged in" + `expiryDesc` (time.Until/since ExpiresAt, "valid for ..."/"expired ... ago"); else if OIDC_ISSUER+CLI_OIDC_CLIENT_ID+CLI_OIDC_CLIENT_SECRET all set -> "client-credentials configured" (env check only, never mints a token); else prints ErrNoToken guidance. Always prints active project, token path, and all three resolved base URLs via the same `client.Resolve*BaseURL` + `MemoryURLForProject` precedence `raw`/`mcp` use (defines local `--operator-base-url`/`--chat-base-url` flags like `mcp`). "not authenticated" exits 0 (informational); only real errors (unreadable config) exit non-zero. No JWT decode, no live validation - deliberately dependency-free and offline-safe. Closes the 0.4.x-class diagnostic gap (handshake "Connected" masking 0 tools / curl-only auth probes). Table-driven `status_test.go` covers four auth states + URL/project/env resolution. NOTE: could not run `make test`/`make lint` in the authoring env (no Go toolchain); relies on branch CI.

2026-06-28 - **X-Request-Id turn-id propagation (tatara-cli#63, SP2-A).** `client.Do()` now stamps `X-Request-Id` from a per-client correlation id read once at `New()` via `correlationID()`: `TATARA_TURN_ID` (the wrapper-minted `turn-<base36>`, injected per turn), fall back to `RUN_ID` (job-style runs), else empty (header omitted). Fixed for the client's lifetime, which is correct because the wrapper relaunches the agent per turn and re-injects the env. This is the single chokepoint behind all three MCP tool groups (memory/operator/chat), so every agent->backend call now carries the turn id; memory and chat already honor an inbound `X-Request-Id` (validators accept `turn-<base36>` verbatim, no transform) and fall back to a fresh id only when absent. Also added `request_id` to the `client.Do` INFO log (SP5-A) for one platform-wide grep key. Approved set was SP1-A/SP2-A/SP3-A/SP4-A/SP5-A; SP3 (wrapper `webhook.post()` header) and SP4 (ingester `push.do()` header) land in `tatara-claude-code-wrapper` / `tatara-memory-repo-ingester` respectively and are follow-ups in those repos (not part of this PR's workspace). This repo carries the linchpin (~80% of value).

2026-07-04 - **Identical tools/list + call-time authz, Component 4a (token-conservation P1).** `NewServer` now registers ALL 68 tools always, regardless of `TATARA_TOOL_PROFILE` - tools/list is byte-identical across every kind, giving every agent pod one shared prompt-cache prefix (tools render first in Anthropic cache order; the old per-kind registration filter fragmented it, one cache lineage per profile). `resolveProfile`'s allow-set is unchanged and now stored on `Server.allow`, enforced in the `register()` dispatch closure at call time instead of at registration: `allow != nil && !allow[name]` returns an authz error before the handler/backend is ever reached (`obs.ToolCallsTotal{tool,"denied"}` + a WARN log). G15 (unknown non-empty profile fails closed to alwaysOn) and the empty-profile fail-open path are both preserved, just moved from list-time to call-time - verified with dedicated tests (denied/alwaysOn/unknown-profile/empty-profile) plus a manual tokenless `tatara mcp` tools/list check (68 tools under both `""` and `refine`). Adjacent fix: `obs.RegisteredTools{profile}` now reports the resolved allow-set size (or full count when nil) instead of the now-always-68 registered count, so the per-profile dashboard signal survives the change.

2026-07-05: push-CD live - agents must not self-merge; release.yml + cd-release cut tags and propagate pins; deploys land via tatara-helmfile deploy-train.

2026-07-12 - SECURITY FIX: an empty TATARA_TOOL_PROFILE previously served the full tool set (fail-open, only a WARN logged). Now fails closed to the alwaysOn set only, matching the unknown-profile behavior. Every tool restriction was advisory until this fix - any agent pod that failed to get TATARA_TOOL_PROFILE injected had unrestricted tool access. Fixing this also broke 6 pre-existing tests (TestE2E_StdioProtocol, TestRegister_LogsInfoOnSuccess/LogsErrorOnFailure/LogsResourceID/MetricsIncremented, TestRegister_204ReturnsOKMarker) not listed in the plan's Cli-Task 1 file list - they used empty-profile servers to call non-alwaysOn memory/chat tools for unrelated plumbing assertions (logging, metrics, 204 handling). Switched their profile arg from "" to "brainstorm" (grants memory+chat unconditionally) rather than changing what they test.

2026-07-12 - Removed the `locked` param from `issue_outcome` (schema + body plumbing in `internal/mcp/tools.go`, locked-only subtests in `tools_test.go`). Implementation-lock/fan-out state is DEFERRED by product: three adversarial review rounds in the operator repo found the lock-clear path unreachable and the close-directive net uncovered for cross-repo refs. `remaining_scope` hard-fail semantics and the empty-profile fail-closed fix (both landed earlier on this branch) are unaffected.

2026-07-12 - P0 FIX (contract L.5): `profiles` was keyed on old Task kinds (triage/lifecycle/...) and had NO `clarify` key, while the operator has always set TATARA_TOOL_PROFILE=clarify. resolveProfile fails closed, so every clarify pod in production ran with six tools and no submit_outcome and its Task could never terminate. Rekeyed the map on the seven AGENT kinds and added `testdata/agent-kinds.txt` as the anti-drift golden (tatara-operator must vendor it byte-identically). The code and memory groups are now profile-gated per contract D.6 instead of unioned into every profile; that shrank refine's allow-set from 19 to 13 and removed memory_edges from brainstorm, so two server_test.go assertions written against the union behaviour were updated. The doc-comment claiming "fail-open" was a lie for the life of the file and is corrected in place.

2026-07-12 - CONTRACT ERRATUM (D.6): the summary counts line ("brainstorm 17, incident 20, clarify 14, implement 17, review 16, refine 13, documentation 19") contradicts the gating table above it. The surface is 20 tools, and incident is denied issue_write + mr_write, so it cannot be 20. Followed the table rows (normative) over the derived counts: 17/18/14/16/15/13/18.

2026-07-12 - MCP surface 74 -> 20 (task-centric contract v4, sections C/D). tools/list is now PER-PROFILE; the byte-identical-tools/list prompt-cache optimisation is deliberately GONE (design accepted risk 3) - submit_outcome's schema is profile-shaped, so identical lists were impossible anyway, and a 17-tool list where all 17 work beats a 74-tool list where 68 authz-error.

2026-07-12 - tatara-chat is decommissioned. TargetChat, ChatTools, HandoffTools, the chat client, DefaultChatBaseURL, --chat-base-url and TATARA_CHAT_URL are gone; NewServer takes 2 clients. Task.status.notes (task_note) is the platform's only agent-to-agent channel.

2026-07-12 - `tatara mcp` refuses to START on a TATARA_CONTRACT_VERSION mismatch (contract G.10). The wrapper image and the operator image are pinned in different helm releases and helmfile applies concurrently, so new-operator + old-agent is reachable; without the handshake each such pod burns 40 Opus turns on a 404ing tool surface, silently.

2026-07-19 - **`mr_takeover_request` tool (task CLI1).** Added the 4th SCM tool (`internal/mcp/tools_scm.go`): POSTs `{repo, number, commentExternalId, task}` to the operator `/projects/{p}/scm/mr-takeover`. Granted to `implement` and `review` profiles only (`profiles.go`). This is a REQUEST, not a merge/approve - the agent judges that a maintainer asked in natural language for takeover and passes that comment's externalId; the operator RE-VALIDATES server-side (comment exists in the mirror, author is an allowed maintainer) and refuses otherwise. No MCP tool exposes merge or approve; agents never merge. Coded against the locked wire contract while the operator endpoint was built in parallel, no live server. Bumped the knock-on static/contract tool-surface counts (19->20 static, 20->21 total) and implement/review profile counts (16->17, 15->16) in `server_test.go`/`tools_test.go`/testdata goldens. semver:minor.

2026-07-26 - **Fleet-wide memory/code-graph 404s (tatara-cli#88/#91).** `TATARA_MEMORY_URL` is not the same shape as the other three base-URL sources: tatara-memory runs as one root-mounted deployment per project, so the operator-injected env var already points at that project's service and carries no path prefix, while the ingress-fronted flag/config-file/default sources are shared and need `/<project>` appended for nginx's rewrite. Replaced the two-step `ResolveBaseURL` + `MemoryURLForProject` at every call site with a single `client.ResolveMemoryURL(flag, env, file, project)` that makes the scoping decision once, so no future call site can append the project onto an already-scoped env base again.

2026-07-26 - **MEMORY_DEGRADED tool guidance + no silent public fallback (P3, pairs with tatara-operator#470).** The nine TargetMemory tools (memory_query/describe/write/entity/edges + code_search/context/graph/explain) now return a structured `MEMORY_DEGRADED` text result instead of a bare transport error, following the `headMovedGuidance` precedent (a normal tool result, not `IsError`: the operator's prompt guidance tells the agent to proceed on reduced recall, and the tool layer must not contradict it). Two triggers: `TATARA_MEMORY_DEGRADED=true` (operator's spawn-time verdict) and an in-process latch (`memoryState`, one per server = one per turn) set by the first failure we see ourselves, which catches an outage starting mid-turn. Once latched, memory tools stop dispatching entirely - otherwise every call burns the client's 60s timeout. Full guidance once, short form after, so one outage does not flood the transcript. **4xx is deliberately NOT latched** (`memoryBackendDown`): a 400/404/409 means the agent's request was wrong, and latching on it would hide a healthy backend behind a MEMORY_DEGRADED banner for the rest of the turn and tell the agent to abandon recall it could still have; 401/403 excluded for the same reason (auth, not memory). Classification needed two new typed errors: `client.TransportError` (wraps only `http.Client.Do` failures, so a missing token or failed refresh is not read as "backend down") and `mcp.statusError` (carries the code; Error() renders exactly what the old fmt.Errorf did). Tools are NOT unregistered when degraded - the surface stays stable so the agent can see what it lost and call report_internal_issue, which keeps working because it takes the local Handler branch BEFORE the degraded gate (it carries the zero-value Target, i.e. TargetMemory). Adjacent fix in the same change: `ResolveMemoryURL` takes os.LookupEnv's ok bool and treats a SET-BUT-EMPTY `TATARA_MEMORY_URL` as "no memory backend" (returns ""), instead of falling through to `DefaultBaseURL` - a pod with no memory stack was silently making cross-cluster calls to the shared public endpoint. UNSET still falls back (workstation use). `tatara mcp` then builds no memory client at all (nil is a supported NewServer arg); `raw` errors explicitly; `status` prints "(not configured...)". Knock-on: `TestRegister_LogsErrorOnFailure` moved from memory_write to task_get, because a memory 5xx is no longer a tool error.

2026-07-19 - added `tatara tool-manifest` (internal/mcp/manifest.go, internal/cmd/toolmanifest.go) and a release.yml step publishing its output as a `tool-manifest.json` release asset (tatara-agent-skills#28). It walks every Tool across all profiles - including all seven submit_outcome schemas via outcomeSchemas - and extracts top-level JSON-schema `enum` arrays per field, keyed by tool name; submit_outcome's per-kind enums (action/verdict/decision) are UNIONED since the manifest is profile-agnostic and one tool name serves all seven. This is the source of truth a new agent-skills CI check (validate_tool_calls.py, sibling change on that repo) fetches to catch a skill naming a stale tool or enum value before it ships. Deliberately shallow (top-level enums only, no nested-object or required-field checking) to match that check's B1 scope.

2026-07-28 - **`exhausted` brainstorm action reachability fix.** The operator (`internal/restapi/outcome.go`) already accepted a third brainstorm action, `exhausted`, but this repo's `brainstormOutcomeSchema` enum and `validateBrainstormOutcome` still only knew `propose|skip`, so no agent could ever emit it (`internal/mcp/outcome.go`). Added `exhausted` to the schema enum and to the skip-shaped validation branch (same non-empty-`reason` gate as `skip`; the operator distinguishes the two purely by the `action` value, no new wire field). Golden `testdata/outcome-brainstorm.schema.json` updated to match - it is compared verbatim in `TestOutcomeTool_SchemaGoldens`.

2026-07-28 - **`approval_citations` added to the clarify outcome schema (agent-judged approval gate, cli Task 10).** `clarifyOutcomeSchema` gains an optional array field, wire key `approvalCitations` (`outcomeArgMap["approval_citations"]`), one `{id,quote}` entry per issue this task owns: `id` is that issue's most recent maintainer comment's `external_id` verbatim from the turn-0 bundle, `quote` a verbatim substring of that comment's body. Item keys are deliberately single words (no `comment_id`) because `outcomeArgMap` only renames TOP-LEVEL keys - a nested snake_case key would reach the operator's `DisallowUnknownFields` decoder unconverted and 400. `validateClarifyOutcome` checks SHAPE only (id/quote non-empty per entry); whether a citation was actually needed, whether it names the most recent maintainer comment, and whether the quote occurs in the body are the operator's re-verification alone (a refusal there is a 200 + park, not a client-side error) - the auto-approve carve-out means a clarify with no maintainer comment legitimately omits the field entirely. Added `TestOutcomeArgMapCoversEverySnakeCaseSchemaKey`, the parity guard this exact entry (2026-06-13, line 24) asked for after `outcomeArgMap` silently drifted from a schema once already; also added a new `outcomeList` helper (`outcomeListLen`'s sibling, returns the slice instead of just its length) since this is the first outcome validator that needs to iterate array items. **CORRECTED below (same day): the "most recent maintainer comment" requirement in this entry was dropped before the operator side shipped.**

2026-07-28 - **`approval_citations` recency requirement dropped post-review (cli Task 10 amendment).** The operator's contract changed after the entry above landed: requiring the cited comment to be the thread's newest maintainer comment deadlocked an ordinary "go ahead, I approve!" followed by an unrelated "thanks - ping me when the PR is up" - the newest maintainer comment is not itself a go-ahead, so no citation could ever be built and the Task parked forever. The operator now verifies (all structural, no recency): the cited comment exists on that issue, its author is a verified non-bot maintainer, the agent's verbatim quote occurs in the body the operator holds, and that comment has not already been consumed as approval evidence; the refusal reason renamed `citation-not-most-recent` -> `citation-not-maintainer`. The veto for a WITHDRAWN approval now lives with the agent: `clarifyOutcomeSchema`'s `approval_citations` description tells it to send `decision=discuss` instead of citing when a later maintainer comment withdraws the approval it would otherwise cite. `validateClarifyOutcome`'s doc comment and shape checks are unchanged (still SHAPE-ONLY, no semantic check added) - only the description string and the comment wording changed. Golden `testdata/outcome-clarify.schema.json` re-synced byte-for-byte.

2026-07-28 - **`outcomeDescriptions["brainstorm"]` was still two-action after the exhausted fix above.** `tools/list` is the first (and for a lazy agent, only) surface that describes a tool, but the top-level description map entry still read "action=propose ... or action=skip ..." with no mention of `exhausted` and no skip-vs-exhausted distinction, even though the schema enum/reason-field/validator all knew about it. Rewrote the entry to name all three actions and restate the skip (transient, this cycle only) vs exhausted (pauses brainstorm scheduling for the whole project, use sparingly) distinction, reusing the exact phrasing already established in the schema's `reason` field description so the two stay in sync in wording, not just in enum membership.

2026-07-30 - release image tag now comes from the cut-tag step output, not `git describe`; a re-run of a release that failed after tagging leaves two semver tags on one commit and describe picks the LOWER (verified: cd-release cuts LIGHTWEIGHT tags with `git tag "$next"`, and for lightweight tags describe tie-breaks by refname, so v3.0.0 beats v3.1.0 regardless of creation order - annotated tags would have gone the other way, which is why this had to be measured not assumed), publishing the image under the previous version while the propagated pin carries the new one. That divergence is the whole bug: `steps.tag.outputs.version` already feeds the wrapper pin bump and the GitHub release, so the image was the one artifact sourced differently. tatara-cli was the LAST repo on the buggy form - chat/wrapper/memory/repo-ingester all carry `VERSION="${VERSION:-...}"` already, tatara-operator inlines the build with a `:?` guard, and tatara-helmfile's cd-release action documents "consumers now take VERSION from this step's output" as the premise for its own re-run guard (helmfile be7e786). Because cli's build.sh is the only one that knows its tag mode, `version` mode also HARD-FAILS on an unset VERSION instead of falling back to describe - a silent fallback there is the bug, not a safety net. Gotcha: bash quote-processes the word in `${VERSION:?word}`, so an apostrophe in that message is a parse error, not text.

2026-08-07 - **contract 3 -> 4: the clarify fold and the implement approval gate (#521, MR4 of seven).** The `clarify` PROFILE is DELETED from tatara-cli, not aliased: leaving `kind=clarify` valid with an optional `approvingMaintainer` would preserve a live path to approval that skips the new gate, which is the exact hole #521 exists to close. `clarifyOutcomeSchema`, `outcomeSchemas["clarify"]`, `outcomeDescriptions["clarify"]`, `validateClarifyOutcome`, `kindProfiles["clarify"]` and `profiles["clarify"]` are all gone, so `resolveProfile` fails CLOSED for a pod still claiming the kind; the operator's one-shot migrator rewrites every live clarify Task to `implement` and deletes the pod. Its three decisions became `action` values on `implementOutcomeSchema`: `implement`->`approved`, `close`->`rejected`, `discuss`->`discuss`, joining `submitted`/`declined`. New wire fields `approvingMaintainer` and `planNoteId` (`outcomeArgMap`); `approvalCitations` moved across unchanged. `documentation` STOPPED sharing `implementOutcomeSchema` in the same change (Task 4.1) - it now has its own `documentationOutcomeSchema` const and `validateDocumentationOutcome`, because implement's action enum grew three gate actions a documentation agent has no operator-side handler for. The implement tool profile absorbed clarify's `issue_write`/`memory_query`/`memory_describe` grants (17 -> 18 tools); it deliberately did NOT gain `task_list` (contract D.6 denies it to implement) or the memory WRITE tools beyond the `memory_write` it already had.

2026-08-07 - **`decline_reason` and the new top-level `reason` collide on the wire; guarded, not merged.** `outcomeArgMap["decline_reason"] = "reason"`, and the implement schema now also carries a top-level `reason` for the gate actions. Both land on `payload["reason"]`, and `buildOutcomePayload` ranges over a map, so accepting both on one call would make the wire value depend on Go's randomised map iteration order - a nondeterministic payload that would reproduce roughly one run in two. `validateImplementOutcome` therefore makes exactly one of the two legal per action: `submitted`/`declined` refuse `reason`, `approved`/`discuss`/`rejected` refuse `decline_reason`. The master plan did not call this out; it is an addition, not a deviation, and `TestOutcome_ImplementReasonAndDeclineReasonNeverCollide` pins it. Related: the submitted/declined arms of the implement validator are shared with documentation via `validateCodeOutcome` rather than copied (the plan said "exact copy") - two hand-maintained copies of one rule silently diverging is the bug class this whole change closes.

2026-08-07 - **`submit_outcome.decision` vanishing from the published manifest is safe for tatara-agent-skills CI, and was checked before relying on it.** `GenerateToolManifest` unions top-level enums across profiles, so deleting the clarify schema also deletes the `decision` field from `tool-manifest.json`. tatara-agent-skills' `validate_tool_calls.py` does `if field not in fields: continue`, i.e. it SKIPS fields the manifest does not track rather than erroring, so the residual `submit_outcome(decision="implement")` prose in the not-yet-updated clarify skill goes inert rather than red. That is what makes the corrected order (MR4 releases before MR3) work in both directions: the new `action` literals are additive and must ship first, and the removed `decision` literals are non-fatal so they can be cleaned up second.

2026-08-07 - **`approving_maintainer` is CONDITIONALLY required, and that is load-bearing for `autoApproveTataraProposals`.** First cut made `reason`, `approving_maintainer` and `plan_note_id` all unconditionally required for `action=approved`. That silently killed the auto-approve carve-out, which is live-enabled on two of the three Projects. The operator grants approval with NO human comment when tatara itself proposed the issue: `approval_grammar.go` takes the carve-out inside the no-maintainer-comment arm and returns `autoApprovalEvidence()`, whose `Login` is the sentinel `<tatara:auto>` (`api/v1alpha1/proposal_marker.go`) with an EMPTY `CommentID`. There is no comment author on that path, so there is no maintainer login the agent could name, and a required field made the whole path unreachable - self-proposed work would stop being implementable. Fixed to the layering the operator already documents at `restapi/outcome.go` for `approvalCitations` ("NOT unconditionally required"): the cli enforces SHAPE, the operator is the trust boundary and enforces AUTHORISATION. Whether auto-approve applies depends on the project flag, the provenance marker and a mirror-vs-`Spec.ProposalBodyHash` comparison, none of which the cli can see, so the cli must accept the omission and let the operator refuse it with a 200 + `granted=false`. What the cli CAN enforce is `checkApprovalPairing`: `approving_maintainer` and `approval_citations` travel together, both or neither - a declared login with no citation has nothing to agree with and would surface as a confusing operator-side `approver-mismatch`. An empty `approval_citations` array counts as absent (it cites nothing); a present-but-blank `approving_maintainer` is refused rather than read as omitted. `plan_note_id` stays unconditionally required: the plan pin is orthogonal to who approved, the agent writes a plan note on the auto-approve path too, and it is still the anti-scope-drift control.

2026-08-13 - **`upgrade` added as the 7th agent kind (Tasks 1.1/1.2 of the upgrade-agent plan). Count-pinned tests extended beyond the plan's own file list.** The plan named `profiles_test.go`/`outcome_test.go` lines to update, but two more count-pinned artifacts in `server_test.go` also key off the profile set and would have gone stale silently: `profileToolCounts` (a hardcoded per-kind tool-count map, needed `"upgrade": 16`) and `TestRegisteredToolGoldens`, which reads a golden file per kind and needed a new `testdata/profile-tools-upgrade.txt`. `TestProfileGatingTable_IsContractD6Verbatim` in `profiles_test.go` also hardcodes a full kind->tools table + count map and required its own `upgrade` row - it is not one of the tests the plan named for renaming, only for existing coverage. `internal/mcp` is a closed universe for this (confirmed by grepping the whole repo for `kindProfiles`/`outcomeProfiles`/`AgentKinds()`/`profileToolCounts`/six-kind literals: all hits are in this one package), so no other package needed touching. Its tool profile deliberately omits issue_write (no approval gate - nobody filed an issue for a scheduled upgrade) and task_list (contract D.6; sibling-unit dedup uses the always-on task_context(index=true)). Its outcome schema is documentationOutcomeSchema reused, not copied. THIS REPO SHIPS BEFORE tatara-operator: operator-ahead-of-cli is the contract L.5 P0 - the pod gets a profile the cli does not know, fails closed to six tools with no submit_outcome, and its Task can never terminate.

2026-08-16 - **`internal/obs` deleted: six Prometheus families, zero series, and the measurement that ruled out the obvious fix (#102).** Every instrumentation site in this repo wrote into a registry nothing ever gathered. The `/metrics` listener was opt-in on `--metrics-addr`/`TATARA_MCP_METRICS_ADDR` and, fleet-wide, NOTHING set it - no pod spec, no chart value, no helmfile value, and `tatara mcp-config` emits no `env` block. Setting it would not have helped: agent pods have no scrape target, and the only egress is the wrapper's push client, which gathers the WRAPPER's registry (a different process) and whose allowed prefixes never included `tatara_mcp_`/`tatara_token_refresh_`/`tatara_client_creds_`. Prometheus confirmed it: `{__name__=~"tatara_mcp.*"}` was empty while `ccw_tool_calls_total` had 46 series. **Do not "fix" this by setting the env var in a pod spec** - that produces a listener on 127.0.0.1 nothing scrapes, which looks wired up and is worse than the honest absence.
  **The stderr route was measured, not assumed, and it is dead.** Probe: a stdio MCP server writing a marker to stderr, registered with `claude mcp list` (claude 2.1.201). The marker did NOT reach claude's own stderr; it landed in `~/.cache/claude-cli-nodejs/<slug>/mcp-logs-<server>/<ts>.jsonl` as `{"error":"Server stderr: ..."}`. The wrapper greps clean for `claude-cli-nodejs|mcp-logs|mcp.log|XDG_STATE_HOME`, and claude runs under a PTY whose ring buffer is dumped only on crash. So a second slog handler on stderr would have been exactly as sealed as the file handler - the shape of the bug being reported. **The one channel that provably escapes this process is the tool result text**, which the wrapper's transcript tailer logs to stdout -> Loki.
  What replaced the metrics: `auth.MintError{Stage, Err}` at all nine client-credentials failure branches (`discovery_request|discovery_call|discovery_status|discovery_decode|token_request|token_call|token_status|token_decode|token_empty`), because collapsing nine causes into `result="error"` left the surviving signal unable to say WHICH call failed; `mcp.WithAuthNote`, which prefixes the mint stage onto ERROR tool results only, so a healthy turn is byte-identical and pays no context tax; and `cfg.Log = logger` on both clients in `mcp.go`, without which `client.go`'s refresh success/failure logging was dead code that nothing ever set. The note also tells the agent to call `report_internal_issue(category="auth")`, which is alwaysOn and needs no auth - that converts an unauthenticated pod into `tatara_wrapper_internal_issue_total`, the one counter on this path that already has an alert.
  **Accepted loss:** `tatara_mcp_tool_call_duration_ms` was the only per-tool latency for the tatara surface, and the `denied`/`degraded`/`ok`/`error` counters go with it. The events still reach Loki as tool_result text and `ccw_tool_calls_total{tool,outcome}` covers ok/error; aggregation and latency do not survive. Re-instrumenting means a real push path (cli + wrapper + operator receiver prefix + helmfile value + observability allowlist, in that order - the receiver prefix must deploy BEFORE the first push or every push is dropped as `reserved_name`), which is why it was not done here.

2026-08-18 - **`pr-not-ready` 409s now render as agent guidance (H1-L), and `discuss` reaches documentation/upgrade (CLI half of H1-B), from `docs/superpowers/plans/2026-08-18-tatara-deliver-end-to-end-plan.md`.** `headMovedGuidance` was a single-reason carve-out (`reason=="head-moved"` only); every other 409 from the operator's `/tasks/{t}/outcome` - including `pr-not-ready` (ci-red, conflict, unresolved-review) - reached the agent as an unparsed JSON blob inside a hard tool error, contradicting `readiness.go:87-92`'s comment claiming otherwise. Generalised into `refusalGuidance(body) (string, bool)`, a reason-keyed renderer: `head-moved` unchanged, `pr-not-ready` decodes `{message, blocked[]}` and appends one line per blocked MR (`repo!number [blockers]: detail`). It stays a CLOSED list - an unrecognised reason (including no reason at all) still returns `ok=false` and stays a hard error; `TestInvoke_UnknownReasonConflictStaysAToolError` and `TestInvoke_GenericConflictWithNoReasonStaysAToolError` pin that (the former replaces the old `TestInvoke_GenericConflictStaysAToolError`, which used a bodyless-reason 409 and did not actually test the closed-list boundary now that a real second reason exists).
  Second, independent change in the same PR (bundled per the plan's C-9/H1-B ordering note - operator must accept `discuss` on documentation/upgrade before this ships): `documentationOutcomeSchema`'s action enum grew `discuss` (shared by `upgrade`, which reuses the const), plus a `reason` schema property. `validateCodeOutcome` (shared by documentation/upgrade and implement's submitted/declined arm) gained a `case "discuss"` requiring non-blank `reason` and refusing every MR-shaped field (`title`/`body`/`change_significance`/`merge_order`/`decline_reason`) the same way `declined` refuses them. This is deliberately NOT the same code path as implement's `discuss` (`validateImplementOutcome`'s own switch intercepts `discuss` before ever calling `validateCodeOutcome`) - the two just happen to converge on "reason required, MR fields forbidden" because that is what pausing means on both schemas. `approved`/`rejected` remain refused on documentation/upgrade; only `discuss` (the pause, not a grant) was added. Two existing tests were amended, not deleted, because they pinned the pre-change contract as intended by the plan: `TestOutcome_UpgradeRefusesTheGateActions` -> `TestOutcome_UpgradeRefusesTheApprovalGateActions` (now excludes discuss from the refused set) and `TestDocumentationSchemaHasNoGateFields` -> `TestDocumentationSchemaHasNoApprovalGateFields` (now asserts `reason` IS reachable, since discuss needs it, while `approving_maintainer`/`plan_note_id`/`approval_citations` stay unreachable). Goldens `testdata/outcome-{documentation,upgrade}.schema.json` resynced.

- 2026-08-23 (tatara-operator#636: the schema advertised `since` for `kind=issues|mr` and `Build` forwarded it for issues only) **`since` is forwarded for `kind=mr`, and `since_days` now goes through `scmInt`, which removes the third integer-coercion shape on one tool.** The operator reads `since` on `/scm/mrs` as of #636, filtered on the forge's `updatedAt`, so the schema description names that field rather than leaving a bare "RFC3339" to imply mirror-refresh semantics. `since_days` reached the query through a bare `argString`, which returns "" for a Go `int`: real JSON-RPC args always decode as `float64`, so it worked in production and was silently droppable for any in-process caller - `number` had already grown `scmNumber` for exactly this, and `scmInt` is that function generalised over the key. `limit`'s description says it selects the NEWEST N by number, because the operator's truncation changed in the same pair of MRs and the tool description is the only place an agent learns it. The cli half merges SECOND: a cli that forwards `since` to an operator that ignores it behaves exactly as before (no error), whereas the reverse is merely inert.
- 2026-08-23 (tatara-operator#636 review round 1) **The cli's scm_read arg table pins its own completeness against the schema literal; the operator's mirror of it pins a count instead.** `scmAdvertisedArgs` is hand-written, so `TestSCMRead_ArgTableCoversEveryOptionalSchemaProperty` derives the expected set from `tl.Schema`'s properties minus `scmReadStructuralArgs` (`kind`, `repo`, `project`, `number` - required, env-resolved, or pinned by their own tests) and asserts BOTH directions: a schema property no row pins, and a row pinning a property the schema dropped. That is reachable here only because the literal is in-package; the operator has to use a hard count for the same job. Verified by mutation - adding an `author` property fails it, removing `labels` from the schema fails it the other way.
- 2026-08-23 (tatara-operator#636 review round 2) **Deriving the arg set is NOT a substitute for pinning the row count: they catch opposite drifts, so `scmAdvertisedArgs` needs both.** The derivation keys coverage by arg, not by `(kind, arg)`, so deleting the `mr`/`since` row leaves `since` "covered" by the `issues` row and the behavioural test simply stops running that case - #636's own drift surviving the guard written for it, reproduced green by mutation. `(kind, arg)` is not derivable: kind applicability lives only in description prose (`"kind=issues|mr only."`, and `state`/`limit` do not even use that shape), and a prose parser would be worse than the disease. `TestSCMRead_AdvertisedArgCountIsPinned` pins 10 rows and rejects a duplicate `kind/arg`, which is the operator's guard verbatim. Rule of thumb for any hand-maintained mirror: derivation catches an ADDED entry, a count catches a DELETED one.
2026-08-23 - **`internal/mcp` is now hermetic against the injected agent-pod environment, and the tool-layer degraded guidance stopped contradicting turn-0 (#105).** `go test ./...` failed 5 tests in every agent pod and passed in CI, because the wrapper hands `os.Environ()` to the test process and `TATARA_MEMORY_DEGRADED=true` short-circuits every `TargetMemory` tool before dispatch - so `server_test.go`/`e2e_test.go`'s faked backends were never called. Third landing of this class: 2026-06-12 patched two tests with `t.Setenv`, 2026-06-13 replaced that with `internal/cmd/main_test.go`'s package `TestMain`, and 2026-07-26 then introduced a NEW injected variable in the one package that still had no `TestMain`.
  `internal/mcp/main_test.go` clears nine vars, derived from what this package's import CLOSURE reads rather than copied from `internal/cmd` - which also clears `XDG_CONFIG_HOME`/`XDG_STATE_HOME`, which move `auth.DefaultTokenPath()` and which the agent runtime does not inject anyway. `TATARA_TOOL_PROFILE` and `TATARA_CONTRACT_VERSION` are deliberately NOT in it: both are read in `internal/cmd/mcp.go` and passed in as parameters, and the `profiles.go` hits are log-message strings. Five of the nine are read one package over: `RUN_ID`/`TATARA_TURN_ID` in `client.correlationID()` which every `freshClient()` constructs, and `OIDC_ISSUER`/`CLI_OIDC_CLIENT_ID`/`CLI_OIDC_CLIENT_SECRET` in `auth.ClientCredsConfigured()`, reached from `internal/client`. All three OIDC vars ARE set in an agent pod, so a test that grew a real token path would mint client_credentials tokens against the live issuer in a pod and not in CI. `TATARA_TURN_ID` is kept defensively only: the wrapper builds the agent's environment from a bare `os.Environ()` and passes `TATARA_TURN_ID` solely as `extraEnv` to the lifecycle-hook subprocess, so in a pod `correlationID()` always falls through to `RUN_ID`. The ten `t.Setenv(memoryDegradedEnv, ...)` calls in `degraded_test.go` are load-bearing, not redundant: `t.Setenv` beats the `TestMain` unset and is the only coverage the gate has - a `TestMain` that neutralises a variable would otherwise hide a regression in the very thing it unsets.
  `TestPackageEnvReadsAreNeutralised` is the part that survives the next six weeks: an AST scan for string literals exactly equal to an agent-runtime-injected name, requiring each to be in the unset list. Exact equality, so `fmt.Errorf("TATARA_CONTRACT_VERSION=%q ...")` and doc comments do not false-positive; literal-based rather than `os.Getenv`-based, so it also catches `argOrEnv(a, "task", "TATARA_TASK")`.
  **It scans the module-local import closure, not a hand-written directory list, and that distinction was itself a review finding.** The first draft listed `{".", "../client"}` - hop 2 of a 3-package closure - which left the guard's own stated invariant FALSE on the commit that introduced it: `internal/client` imports `internal/auth`, whose three OIDC reads were unenforced and set in every pod. Inert only because nothing on the mcp path calls `AccessTokenWithExpiry` yet. Stopping at any fixed hop is arbitrary; `scannedDirs` now BFS-walks imports with the `github.com/szymonrychu/tatara-cli/` prefix from `.` outward, so hop 4 enforces itself. A floor assertion pins that the walk actually resolves imports (a typo'd module prefix would otherwise silently shrink the scan back to `.` and pass). Verified failable at every hop by dropping a var from the list: `TATARA_PROJECT` names `tools.go:140:33`, `RUN_ID` names `../../internal/client/client.go:69:19`, `OIDC_ISSUER` names `../../internal/auth/clientcreds.go:136:22`. Note `parser.ParseDir` is deprecated as of go 1.26 and trips `staticcheck` - the walk parses each file with `parser.ParseFile(..., parser.ImportsOnly)` instead.
  Second half: `newMemoryState` now reads `TATARA_MEMORY_DISABLED` and the guidance splits by WHO KNOWS, not by which env var. The two env arms (`DISABLED=true`, `DEGRADED=true`) render `knownGuidance`, which drops the `report_internal_issue` instruction - the operator's own `promptguidance.MemoryDegradedGuidance` AND `MemoryDisabledGuidance` both already tell the agent at turn 0 NOT to report it (tatara-operator#523), so the tool text was contradicting the prompt and manufacturing a duplicate alert per turn. Two arms KEEP the report, and for the same reason: the mid-turn `latch()` (the operator said healthy at spawn, so this process is the only witness) and `!configured` (an empty endpoint while memory is neither disabled nor flagged is a state nothing upstream has named, and off-pod there is no operator to have named it). The `MEMORY_DEGRADED:` / `MEMORY_DEGRADED (see earlier):` prefixes are unchanged - 12 sites in `tatara-agent-skills` match on that literal.
  **The switch order is the whole correctness of the disabled arm, and it reads backwards on purpose.** Disabling memory on a Project clears `status.memory.endpoint` (`project_memory_disable.go:69`), so a disabled pod gets `TATARA_MEMORY_URL=""` -> nil memory client -> `newMemoryState(false)`, i.e. ALL THREE conditions true at once. The first draft tested `!configured` first, which made the arm it had just added dead code in the only state it exists for: on this very project the agent still read "TATARA_MEMORY_URL is set but empty", telling it the pod was misconfigured when it was behaving exactly as configured. Caught by an adversarial architect review of the commit, not by the tests, because the test for the new arm passed a NON-nil memory client - a combination production never produces. It is now a two-case table (nil = steady state, non-nil = the spec-vs-status skew window) and both assert the disabled reason.
  A follow-up review found the order was pinned in only two of three directions. The disabled test's `NotContains(text, "flagged it unhealthy")` is what proves it did not fall through to the DEGRADED arm, but that literal had zero POSITIVE coverage - rewording the DEGRADED reason left the whole package green while that assertion started passing vacuously, switch order reverted or not. `TestCallTool_DegradedEnvReturnsGuidanceWithoutCallingBackend` now asserts `Contains(text, "flagged it unhealthy")`. General shape worth keeping: an assertion that a string is ABSENT is only load-bearing while something else pins that the string is what the other branch actually renders.
  **Left open, deliberately:** `tatara-agent-skills` (`tatara-mcp-memory` step 2, and the carve-out copies in `tatara-evidence-and-citation`, `tatara-platform-contract`, `tatara-council-brainstorm`, `tatara-deep-architectural-research`, `tatara-mcp-code-graph`) still instructs `report_internal_issue(severity="warn")` on ANY `MEMORY_DEGRADED` result. That text is now correct only for the latch case. It already contradicted turn-0 before this change; it is a skills-repo change, outside #105's approved scope.
